import 'package:flutter/material.dart';
import '../Components/Host/host_field.dart';

class HostAlert extends StatelessWidget {
  const HostAlert({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return getMyAlert(context);
  }
}
